// Rule 3: Different terms for inputs

function compressBoxes(boxes: string[], boxes2: string[]) {
    boxes.forEach(box => {
        // O(n)
        console.log(box)
    })

    boxes2.forEach(box => {
        // O(m)
        console.log(box)
    })
}

// O(n+m)
